package model.decorator;

import java.awt.*;

public class BaseRainbow implements Rainbow {
    @Override
    public void draw(Graphics g, int w, int h) {
        // Базовая реализация: ничего не рисует
    }
}
